<% if @subnavigation == true %>
	<div class="sub-top-bar">
		<div class="header-submenu">
	  	<ul class="header-submenu-list">
	      <li class="header-submenu-item header-nom-edifici"><%= @edifici.nom_edifici %></li>
	  		<li class="header-submenu-item <% if @submenu_actiu == 'identificacio' %>
	            header-submenu-actiu
	            <% end %>"><%= link_to(t('.identificacio'), edit_identificacio_path(@edifici.identificacio.id), :class => 'header-submenu-link') %></li>
	      <li class="header-submenu-item <% if @submenu_actiu == 'operacions' %>
	            header-submenu-actiu
	            <% end %>"><%= link_to(t('.operacions'), operacions_path(edifici_id: @edifici.id), :class => 'header-submenu-link') %></li>
	      
	      <li class="header-submenu-item <% if @submenu_actiu == 'fases' %>
	            header-submenu-actiu
	            <% end %>"><%= link_to(t('.fases'), fases_path(edifici_id: @edifici.id), :class => 'header-submenu-link') %></li>
	      <li class="header-submenu-item <% if @submenu_actiu == 'planificacio' %>
	            header-submenu-actiu
	            <% end %>"><%= link_to(t('.planificacio'), edit_planificacio_path(id: @edifici.planificacio.id, edifici_id: @edifici.id), :class => 'header-submenu-link') %></li>
	      <li class="header-submenu-item <% if @submenu_actiu == 'document' %>
	            header-submenu-actiu
	            <% end %>"><%= link_to(t('.document'), document_path(edifici_id: @edifici.id), :class => 'header-submenu-link') %></li>
	  	</ul>
    </div>
	</div>
<% end %>
